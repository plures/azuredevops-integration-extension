#!/bin/sh
# Run validation checks before commit
npm run validate:all || exit 1
npm run test:feature || exit 1

# Check architecture discipline compliance
npm run check:architecture || exit 1

# Run lint-staged for formatting and linting
npx lint-staged
